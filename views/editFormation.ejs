<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier la Formation</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h1 class="text-center">Modifier la Formation</h1>
    <h2 class="text-center mb-4"><%= supermarket.name %></h2>
    <form action="/supermarket/<%= supermarket._id %>/formations/<%= formation._id %>/edit" method="POST" class="card p-4 shadow">
      <div class="mb-3">
        <label class="form-label">Nombre de formations :</label>
        <input type="number" name="nombreFormation" class="form-control" value="<%= formation.nombreFormation %>" min="1" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Type de formation :</label>
        <select name="typeFormation" class="form-select" required>
          <option value="Incendie" <%= formation.typeFormation === 'Incendie' ? 'selected' : '' %>>Incendie</option>
          <option value="SST" <%= formation.typeFormation === 'SST' ? 'selected' : '' %>>SST</option>
          <option value="Integration" <%= formation.typeFormation === 'Integration' ? 'selected' : '' %>>Integration</option>
          <!-- Add additional options if needed -->
        </select>
      </div>
      <div class="mb-3">
        <label class="form-label">Date de formation :</label>
        <input type="date" name="dateFormation" class="form-control" value="<%= formation.dateFormation ? formation.dateFormation.toISOString().split('T')[0] : '' %>">
      </div>
      <button type="submit" class="btn btn-primary">Mettre à jour</button>
      <a href="/supermarket/<%= supermarket._id %>/formations" class="btn btn-secondary">Annuler</a>
    </form>
  </div>
</body>
</html>
