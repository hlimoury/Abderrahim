<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier Accident de Travail</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h1 class="text-center">Modifier l'Accident de Travail</h1>
    <h2 class="text-center mb-4"><%= supermarket.name %></h2>

    <form
      action="/supermarket/<%= supermarket._id %>/accidents/travail/<%= accident._id %>/edit"
      method="POST"
      class="card p-4 shadow"
    >
      <div class="mb-3">
        <label class="form-label">Numéro :</label>
        <input
          type="number"
          name="numeroAccident"
          class="form-control"
          value="<%= accident.numeroAccident %>"
          min="1"
        >
      </div>
      <div class="mb-3">
        <label class="form-label">Accident déclaré ?</label>
        <input
          type="checkbox"
          name="estDeclare"
          <%= accident.estDeclare ? 'checked' : '' %>
        >
      </div>
      <div class="mb-3">
        <label class="form-label">Nombre de jours d'arrêt :</label>
        <input
          type="number"
          name="nombreJoursArret"
          class="form-control"
          value="<%= accident.nombreJoursArret %>"
          min="0"
        >
      </div>
      <div class="mb-3">
        <label class="form-label">Cause :</label>
        <input
          type="text"
          name="cause"
          class="form-control"
          value="<%= accident.cause %>"
        >
      </div>
      <div class="mb-3">
        <label class="form-label">Date de l'accident :</label>
        <input
          type="date"
          name="dateAccident"
          class="form-control"
          value="<%= accident.dateAccident.toISOString().split('T')[0] %>"
        >
      </div>
      <button type="submit" class="btn btn-primary">Mettre à jour</button>
      <a href="/supermarket/<%= supermarket._id %>/accidents" class="btn btn-secondary">Retour</a>
    </form>
  </div>
</body>
</html>
